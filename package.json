{
  "name": "temperature",
  "version": "1.0.0",
  "description": "Temperature",
  "private": true,
  "workspaces": [
    "api",
    "model",
    "ui"
  ],
  "scripts": {
    "build": "lerna run build:children && lerna run prepare-files",
    "prepare-files":"mkdir -p dist/static && cp api/dist/*.js dist && cp -R ui/build/* dist/static",
    "build:children": "lerna exec yarn build",
    "start": "cd api && yarn start"
  },
  "author": "Hirle",
  "license": "UNLICENSED",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
